@model AeroDroxUAV.Models.Drone
@{
    ViewData["Title"] = "Add New Drone";
}

<div class="container py-3">
    <div class="row justify-content-center">
        <div class="col-12">
            <div class="card shadow border-0">
                <div class="card-header bg-gradient-primary text-white py-3">
                    <div class="d-flex align-items-center">
                        <div class="me-3">
                            <i class="fas fa-drone fa-2x"></i>
                        </div>
                        <div>
                            <h1 class="h4 mb-0">Add New Drone</h1>
                            <p class="mb-0 opacity-75 small">Enter the drone details below</p>
                        </div>
                    </div>
                </div>
                <div class="card-body p-4">
                    <form method="post" enctype="multipart/form-data" id="droneForm">
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <div class="form-floating">
                                    <input type="text" asp-for="Name" class="form-control form-control-sm" 
                                           id="name" placeholder="Enter drone name" required />
                                    <label for="name" class="small">
                                        <i class="fas fa-signature me-2"></i>Drone Name
                                    </label>
                                    <span asp-validation-for="Name" class="text-danger small"></span>
                                </div>
                            </div>
                            
                            <div class="col-md-6 mb-3">
                                <div class="form-floating">
                                    <select asp-for="Category" class="form-select form-select-sm" id="category" required>
                                        <option value="">Select Category</option>
                                        <option value="Agriculture">Agriculture</option>
                                        <option value="Defence">Defence</option>
                                        <option value="Survelliance">Survelliance</option>
                                        <option value="Mapping">Mapping</option>
                                        <option value="Photography">Photography</option>
                                        <option value="DJI">DJI</option>
                                        <option value="Customize">Customize</option>
                                    </select>
                                    <label for="category" class="small">
                                        <i class="fas fa-layer-group me-2"></i>Category
                                    </label>
                                    <span asp-validation-for="Category" class="text-danger small"></span>
                                </div>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <div class="form-floating">
                                    <input type="number" asp-for="Price" step="0.01" min="0"
                                           class="form-control form-control-sm" id="price" 
                                           placeholder="Enter price" required />
                                    <label for="price" class="small">
                                        <i class="fas fa-rupee-sign me-2"></i>Original Price (₹)
                                    </label>
                                    <span asp-validation-for="Price" class="text-danger small"></span>
                                </div>
                            </div>
                            
                            <div class="col-md-6 mb-3">
                                <div class="form-floating">
                                    <input type="number" asp-for="DiscountPrice" step="0.01" min="0"
                                           class="form-control form-control-sm" id="discountPrice" 
                                           placeholder="Enter discount price (optional)" />
                                    <label for="discountPrice" class="small">
                                        <i class="fas fa-percentage me-2"></i>Discount Price (₹) (Optional)
                                    </label>
                                    <div class="form-text small">
                                        Only show if less than original price
                                    </div>
                                    <span asp-validation-for="DiscountPrice" class="text-danger small"></span>
                                </div>
                            </div>
                        </div>

                        <!-- Condition Dropdown -->
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <div class="form-floating">
                                    <select asp-for="Condition" class="form-select form-select-sm" id="condition" required>
                                        <option value="">Select Condition</option>
                                        <option value="New">New</option>
                                        <option value="Refurbished">Refurbished</option>
                                        <option value="Used">Used</option>
                                    </select>
                                    <label for="condition" class="small">
                                        <i class="fas fa-clipboard-check me-2"></i>Condition
                                    </label>
                                    <span asp-validation-for="Condition" class="text-danger small"></span>
                                </div>
                            </div>
                            
                            <div class="col-md-6 mb-3">
                                <div class="form-floating">
                                    <input type="number" asp-for="StockQuantity" class="form-control form-control-sm" 
                                           id="stock" placeholder="Stock quantity" min="0" required />
                                    <label for="stock" class="small">
                                        <i class="fas fa-boxes me-2"></i>Stock Quantity
                                    </label>
                                    <span asp-validation-for="StockQuantity" class="text-danger small"></span>
                                </div>
                            </div>
                        </div>

                        <!-- Image Upload -->
                        <div class="row mb-3">
                            <div class="col-12">
                                <div class="form-group">
                                    <label for="imageFile" class="form-label fw-semibold small">
                                        <i class="fas fa-image me-2"></i>Drone Image
                                    </label>
                                    <input type="file" asp-for="ImageFile" class="form-control form-control-sm" 
                                           id="imageFile" accept="image/*">
                                    <div class="form-text small">
                                        Upload a high-quality image of the drone (Reccomenadtion: JPG, PNG, max 5MB, sqaure size (512 x 512 pixels) for best view)
                                    </div>
                                    <span asp-validation-for="ImageFile" class="text-danger small"></span>
                                </div>
                                <div id="imagePreview" class="mt-2 text-center d-none">
                                    <img id="previewImage" class="img-thumbnail" style="max-height: 150px;" />
                                </div>
                            </div>
                        </div>

                        <div class="mb-3">
                            <div class="form-floating">
                                <textarea asp-for="Description" class="form-control form-control-sm" 
                                          id="description" placeholder="Enter description"
                                          style="height: 100px;"></textarea>
                                <label for="description" class="small">
                                    <i class="fas fa-align-left me-2"></i>Description
                                </label>
                            </div>
                        </div>

                        <!-- Checkboxes Section -->
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <div class="form-check form-switch mt-3">
                                    <input class="form-check-input" type="checkbox" asp-for="IsFeatured" id="featured">
                                    <label class="form-check-label fw-semibold small" for="featured">
                                        <i class="fas fa-star me-2"></i>Customize Build Drone
                                    </label>
                                    <div class="form-text small">
                                        Customized Build drones appear on the home page
                                    </div>
                                </div>
                            </div>
                            
                            <div class="col-md-6 mb-3">
                                <div class="form-check form-switch mt-3">
                                    <input class="form-check-input" type="checkbox" asp-for="ShowOnHomepage" id="showOnHomepage">
                                    <label class="form-check-label fw-semibold small" for="showOnHomepage">
                                        <i class="fas fa-home me-2"></i>Add to Home Page
                                    </label>
                                    <div class="form-text small">
                                        Show this drone in New Arrivals section on homepage
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <div>
                                <button type="submit" class="btn btn-primary px-4">
                                    <i class="fas fa-save me-2"></i>Add Drone
                                </button>
                            </div>
                            <div>
                                <a href="/Drone/Index" class="btn btn-outline-secondary px-4">
                                    <i class="fas fa-times me-2"></i>Cancel
                                </a>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            // Image preview
            $('#imageFile').change(function() {
                const file = this.files[0];
                if (file) {
                    const reader = new FileReader();
                    reader.onload = function(e) {
                        $('#previewImage').attr('src', e.target.result);
                        $('#imagePreview').removeClass('d-none');
                    }
                    reader.readAsDataURL(file);
                } else {
                    $('#imagePreview').addClass('d-none');
                }
            });

            // Discount price validation
            $('#discountPrice').on('blur', function() {
                const originalPrice = parseFloat($('#price').val()) || 0;
                const discountPrice = parseFloat($(this).val()) || 0;
                
                if (discountPrice > 0 && discountPrice >= originalPrice) {
                    $(this).addClass('is-invalid');
                    $(this).next('.form-text').addClass('text-danger');
                    $(this).next('.form-text').text('Discount price must be less than original price');
                } else {
                    $(this).removeClass('is-invalid');
                    $(this).next('.form-text').removeClass('text-danger');
                    $(this).next('.form-text').text('Only show if less than original price');
                }
            });

            // Form validation
            $('#droneForm').submit(function(e) {
                e.preventDefault();
                
                var isValid = true;
                $(this).find('[required]').each(function() {
                    if ($(this).val() === '') {
                        $(this).addClass('is-invalid');
                        isValid = false;
                    } else {
                        $(this).removeClass('is-invalid');
                    }
                });
                
                // Validate discount price
                const originalPrice = parseFloat($('#price').val()) || 0;
                const discountPrice = parseFloat($('#discountPrice').val()) || 0;
                
                if (discountPrice > 0 && discountPrice >= originalPrice) {
                    $('#discountPrice').addClass('is-invalid');
                    isValid = false;
                }
                
                if (isValid) {
                    this.submit();
                }
            });
            
            // Real-time validation
            $('input[required], select[required], textarea[required]').on('blur', function() {
                if ($(this).val() === '') {
                    $(this).addClass('is-invalid');
                } else {
                    $(this).removeClass('is-invalid');
                }
            });
            
            // Price formatting
            $('#price, #discountPrice').on('blur', function() {
                var value = parseFloat($(this).val());
                if (!isNaN(value) && value >= 0) {
                    $(this).val(value.toFixed(2));
                }
            });
        });
    </script>
    
    <style>
        .bg-gradient-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        
        .card {
            border-radius: 12px;
            overflow: hidden;
        }
        
        .card-header {
            border-radius: 12px 12px 0 0 !important;
        }
        
        .form-floating {
            position: relative;
        }
        
        .form-control-sm, .form-select-sm {
            border-radius: 8px;
            border: 2px solid #e0e0e0;
            padding: 14px 10px 2px 10px;
            font-size: 0.875rem;
            transition: all 0.3s ease;
        }
        
        .form-control-sm:focus, .form-select-sm:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.15rem rgba(102, 126, 234, 0.25);
        }
        
        .form-control-sm.is-invalid {
            border-color: #dc3545;
        }
        
        .form-check-input:checked {
            background-color: #667eea;
            border-color: #667eea;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border: none;
            border-radius: 8px;
            padding: 8px 20px;
            font-weight: 600;
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }
        
        .btn-primary:hover {
            background: linear-gradient(135deg, #764ba2 0%, #667eea 100%);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
        }
        
        .btn-outline-secondary {
            border-radius: 8px;
            padding: 8px 20px;
            border-width: 2px;
            font-size: 0.9rem;
        }
        
        .form-text {
            font-size: 0.8rem;
            color: #6c757d;
            margin-top: 3px;
        }
        
        .small {
            font-size: 0.875rem;
        }
        
        label {
            font-weight: 500;
        }
    </style>
}